# Use left-handed world coordinates

Use a left-handed world coordinate system with `+Y` up and `+Z` forward into the scene. This makes forward depth intuitive while learning the graphics pipeline; the trade-off is that right-handed assets and conventions, including glTF content, will require an explicit coordinate conversion at the import boundary.
